[project]
name = "bookrec"
version = "0.1.0"
description = "Semantic book recommender (vector search + filters + Gradio UI)"
requires-python = ">=3.11"
dependencies = []

[tool.ruff]
line-length = 100
